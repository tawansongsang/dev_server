volumes:
  python_dev:
services:
  ptyhon_aiml:
    build:
      context: .
      dockerfile: docker/fedora/python/Dockerfile.python-aiml
    restart: unless-stopped
    command: [ "tail", "-f", "/dev/null" ]
    volumes:
      - python_dev:/root/workspace
